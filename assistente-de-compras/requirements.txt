# Dependencias do assistente de compras (Python 3.10+)
requests>=2.31          # download das paginas e chamadas a Bot API do Telegram
beautifulsoup4>=4.12    # leitura do HTML
lxml>=5.0               # parser rapido usado pelo BeautifulSoup quando disponivel
python-dotenv>=1.0      # carrega o arquivo .env

# Opcional: so instale se alguma loja montar o preco via JavaScript.
#   pip install playwright && playwright install chromium
# playwright>=1.45
